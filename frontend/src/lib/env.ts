/**
 * Variables de entorno del desktop.
 * apiUrl vacío: el desktop no usa un Vercel API propio para billing —
 * el checkout se maneja vía handoff a maity.cloud (ver lib/planLinks.ts).
 */
export const env = {
  apiUrl: '',
}
