'use client';
/**
 * STUB desktop de `AnimalPortrait` (web Sixale730/maity@3ef2914
 * src/features/avatar/components/AnimalPortrait.tsx).
 *
 * El avatar animal studio está FUERA de alcance del desktop (decisión de Julio,
 * sep-2026): no se embarcan los `*-base-v1.webp` de los animales. `ExpeditionReward`
 * (copia tal cual) lo importa, así que existe con la misma firma y no pinta nada.
 */
import type { AnimalDesign } from '@maity/shared';

// eslint-disable-next-line @typescript-eslint/no-unused-vars -- misma firma que la web
export function AnimalPortrait(_props: { design: AnimalDesign; className?: string; scene?: boolean }) {
  return null;
}
